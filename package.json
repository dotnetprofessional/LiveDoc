{
  "scripts": {
    "watch": "tsc -b --watch packages",
    "pack": "lerna run pack",
    "alpha-patch": "lerna version prerelease --no-git-tag-version --no-push"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/mocha"
    ]
  },
  "private": true,
  "devDependencies": {
    "lerna": "^3.7.1"
  }
}