# Mal — Playwright Bug Fix Code Review

**Timestamp:** 2026-04-12T07:34:59Z  
**Agent:** Mal (Lead Code Reviewer)  
**Model:** claude-sonnet-4.5  
**Mode:** background  

## Session Summary

Reviewed Playwright bug fix implementation (Bug 1: freshContextPerScenario, Bug 2: close timeout) and Zoe's test coverage. Approved both fixes with one medium-priority finding.

## Findings

### ✅ APPROVED
- `freshContextPerScenario` architecture is sound. Browser context isolation works correctly.
- `browser.close()` cascades properly through cleanup handlers.
- `onScenarioStart`/`onScenarioEnd` public exports are well-designed for future plugins.

### ⚠️ Medium Finding: scenarioEndHooks error isolation
- **File:** `packages/vitest/_src/app/livedoc.ts`, lines 418-420 and 655-657
- **Issue:** No try/catch per hook. If one hook throws, subsequent hooks and afterBackground cleanup are skipped.
- **Recommendation:** Wrap each hook in try/catch, collect errors, throw after all hooks run (or at minimum log and continue).

### ℹ️ Low Finding: Module-level hook arrays never cleared
- **File:** `packages/vitest/_src/app/livedoc.ts`, lines 96-97
- **Issue:** `scenarioStartHooks` and `scenarioEndHooks` are singletons that grow and are never cleared.
- **Note:** Safe under vitest's default threading (each file = own worker = own module state).
- **Recommendation:** Document the single-worker assumption, or track registrations to prevent duplicates.

## Decision

**Status:** APPROVED with findings  
See `.squad/decisions/inbox/mal-playwright-review.md` for full decision record.

## Next Steps

- Address medium finding before or shortly after merge.
- Document low-priority finding and single-worker threading assumption.
